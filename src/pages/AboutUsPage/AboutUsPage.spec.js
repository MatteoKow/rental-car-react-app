import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import AboutUsPage from './AboutUsPage';

describe('AboutUsPage', () => {
  test('renders AboutUsPage with proper content', () => {
    // render(<AboutUsPage />);
    
    // const aboutUsPageContainer = document.getElementsByClassName('about-us-page')[0];
    // expect(aboutUsPageContainer).toBeInTheDocument();


    // const contactPanel = aboutUsPageContainer.querySelector('.contact-panel');
    // expect(contactPanel).toBeInTheDocument();

    // const titleElement = contactPanel.querySelector('.title');
    // expect(titleElement).toBeInTheDocument();
    // expect(titleElement.textContent).toBe('O nas');

    // const textInfoElement = contactPanel.querySelector('.text-info');
    // expect(textInfoElement).toBeInTheDocument();

    // const contactDetails = contactPanel.querySelector('.contact-panel-details');
    // expect(contactDetails).toBeInTheDocument();


    // const contactElements = contactDetails.querySelectorAll('span');
    // expect(contactElements.length).toBe(5);
  });
});
